/*
vim: syntax=groovy
-*- mode: groovy;-*-
 * -------------------------------------------------
 * Nextflow config file
 * -------------------------------------------------
 * Config options for Docker.
 */

docker {
  enabled = true
  fixOwnership = true
}

process.container = "maxulysse/compile-beamer:${params.tag}"
