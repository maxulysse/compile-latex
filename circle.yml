machine:
  java:
    version: openjdk7
  services:
    - docker
    
test:
  override:
    - curl -fsSL get.nextflow.io | bash
    - ./nextflow run main.nf -with-docker -profile docker --tex sample.tex