machine:
  java:
    version: openjdk8

  services:
    - docker

  environment:
    NXF_VER: 0.23.0

dependencies:
  override:
  - docker pull maxulysse/compile-beamer:1.2
  - curl -fsSL get.nextflow.io | bash

test:
  post:
    - ./nextflow run . -with-docker -profile docker
